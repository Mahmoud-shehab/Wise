# تشخيص مشكلة لوحة المراجعة

## المشكلة
المهمة لا تظهر في لوحة المراجعة رغم تعيين المراجع

## السبب المحتمل
**لم يتم تطبيق Migration في قاعدة البيانات!**

حقل `reviewer_id` غير موجود في جدول `tasks` في Supabase

## الحل

### الخطوة 1: تطبيق Migration
1. افتح Supabase Dashboard
2. اذهب إلى **SQL Editor**
3. انسخ محتوى الملف: `supabase/migrations/20240107_add_review_workflow.sql`
4. الصق المحتوى في SQL Editor
5. اضغط **RUN**

### الخطوة 2: التحقق من نجاح Migration
بعد تشغيل Migration، شغّل هذا الاستعلام للتحقق:

```sql
-- التحقق من وجود حقل reviewer_id
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'tasks' 
AND column_name = 'reviewer_id';
```

يجب أن يظهر:
```
column_name  | data_type
reviewer_id  | uuid
```

### الخطوة 3: إعادة تعيين المراجع
بعد تطبيق Migration:
1. افتح المهمة من صفحة تفاصيل المهمة
2. اختر المراجع من القائمة المنسدلة
3. سيتم حفظه تلقائياً

### الخطوة 4: التحقق من لوحة المراجعة
1. سجل الدخول بحساب المراجع
2. افتح "لوحة المراجعة"
3. يجب أن تظهر المهمة في قسم "قيد التنفيذ"

## التحقق من Console
افتح Developer Tools (F12) وانظر إلى Console:
- يجب أن ترى: `🔍 Fetching review tasks for user: [user_id]`
- يجب أن ترى: `📊 Review tasks data: [...]`

إذا كان `data` فارغ `[]`، فهذا يعني:
1. إما لم يتم تطبيق Migration
2. أو لم يتم حفظ `reviewer_id` في المهمة

## ملاحظات مهمة

### ⚠️ Migration إلزامي!
بدون تطبيق Migration، لن يعمل نظام المراجعة لأن:
- حقل `reviewer_id` غير موجود
- حقل `reviewed_at` غير موجود
- حالة `pending_review` غير مضافة
- Triggers الإشعارات غير موجودة

### ✅ بعد تطبيق Migration
- يمكن تعيين مراجع لكل مهمة
- المهام تظهر في لوحة المراجعة
- الإشعارات تعمل تلقائياً
- النظام يعمل بشكل كامل

## استعلامات مفيدة للتشخيص

### عرض جميع المهام مع المراجع
```sql
SELECT id, title, assignee_id, reviewer_id, status
FROM tasks
ORDER BY created_at DESC
LIMIT 10;
```

### عرض المهام التي لها مراجع
```sql
SELECT t.id, t.title, t.reviewer_id, p.full_name as reviewer_name
FROM tasks t
LEFT JOIN profiles p ON t.reviewer_id = p.id
WHERE t.reviewer_id IS NOT NULL;
```

### تحديث مراجع لمهمة معينة يدوياً (للاختبار)
```sql
UPDATE tasks
SET reviewer_id = 'USER_ID_HERE'
WHERE id = 'TASK_ID_HERE';
```

## الخلاصة

**المشكلة الرئيسية**: لم يتم تطبيق Migration!

**الحل**: طبق Migration من ملف `supabase/migrations/20240107_add_review_workflow.sql`

بعد ذلك، كل شيء سيعمل بشكل صحيح! 🎉
