# 🔧 إصلاح مشكلة إرسال الرسائل

## المشكلة:
عند محاولة إرسال رسالة، يظهر خطأ:
```
⚠️ لا توجد صلاحيات كافية (RLS Policy)
```

## السبب:
جدول الرسائل (`messages`) محتاج صلاحيات (RLS Policies) عشان الموظفين يقدروا يبعتوا رسائل.

---

## ✅ الحل (خطوة واحدة):

### 1. افتح Supabase SQL Editor:
```
https://supabase.com/dashboard/project/[project-id]/sql
```

### 2. انسخ والصق الكود من الملف:
```
fix_messages_rls_policy.sql
```

### 3. اضغط "Run" أو "تشغيل"

### 4. انتظر رسالة النجاح:
```
✅ تم إصلاح صلاحيات جدول الرسائل بنجاح!
```

---

## 📋 ما الذي سيحدث:

الكود هيعمل الآتي:
1. ✅ حذف الـ policies القديمة (لو موجودة)
2. ✅ إعادة تفعيل RLS على جدول الرسائل
3. ✅ إنشاء policy للقراءة (المستخدم يشوف رسائله فقط)
4. ✅ إنشاء policy للإرسال (أي مستخدم يقدر يبعت رسالة)
5. ✅ إنشاء policy للتحديث (المستخدم يعلم رسائله مقروءة)
6. ✅ إنشاء policy للحذف (المستخدم يحذف رسائله)

---

## 🎯 بعد تطبيق الحل:

1. ارجع للتطبيق
2. جرب تبعت رسالة تاني
3. المفروض تشتغل بدون مشاكل! ✅

---

## 🔐 الصلاحيات الجديدة:

| العملية | الصلاحية |
|---------|----------|
| **القراءة** | المستخدم يشوف الرسائل اللي هو مرسلها أو مستقبلها فقط |
| **الإرسال** | أي مستخدم مسجل دخول يقدر يبعت رسالة |
| **التحديث** | المستخدم يقدر يعلم الرسائل اللي استقبلها كـ "مقروءة" |
| **الحذف** | المستخدم يقدر يحذف رسائله (المرسلة أو المستقبلة) |

---

## 🆘 لو المشكلة لسه موجودة:

1. **تأكد إن الكود اتنفذ بنجاح:**
   - شوف رسالة النجاح في SQL Editor
   - لو في أخطاء، ابعتها لي

2. **تأكد إن المستخدم مسجل دخول:**
   - لازم يكون في session صحيحة
   - جرب تسجل خروج ودخول تاني

3. **امسح الـ cache:**
   - شوف ملف `CLEAR_CACHE_INSTRUCTIONS.md`

---

**ملاحظة مهمة:** 
هذا الحل آمن ومش هيأثر على البيانات الموجودة. بس هيصلح الصلاحيات عشان المستخدمين يقدروا يستخدموا نظام الرسائل.

---

**آخر تحديث:** يناير 2026
