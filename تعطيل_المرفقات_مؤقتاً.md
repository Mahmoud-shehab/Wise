# حل مؤقت: تعطيل المرفقات

## الخيار 1: إخفاء قسم المرفقات

إذا لم تكن بحاجة للمرفقات الآن، يمكنك إخفاء القسم:

### في ملف `src/routes/TaskDetailsPage.tsx`:

ابحث عن هذا السطر:
```tsx
<TaskAttachments taskId={task.id} />
```

وعلّق عليه (أضف `{/* */}` حوله):
```tsx
{/* <TaskAttachments taskId={task.id} /> */}
```

## الخيار 2: إنشاء الـ Bucket يدوياً

### الطريقة الصحيحة:

1. **افتح Supabase Dashboard**
   - https://supabase.com/dashboard

2. **اذهب إلى Storage**
   - من القائمة الجانبية: Storage

3. **تحقق من الـ Buckets الموجودة**
   - هل ترى bucket اسمه "attachments"؟
   - إذا نعم، المشكلة في الـ policies
   - إذا لا، أنشئه

4. **إنشاء Bucket جديد**
   - اضغط "New bucket"
   - Name: `attachments` (بالأحرف الصغيرة، بدون مسافات)
   - Public: ✅ نعم
   - اضغط Create

5. **تحقق من الاسم**
   - يجب أن يكون بالضبط: `attachments`
   - ليس: "Attachments" أو "attachment" أو "attachments "

## الخيار 3: استخدام bucket مختلف

إذا كان لديك bucket آخر، يمكنك تغيير الاسم في الكود:

### في ملف `src/components/task/TaskAttachments.tsx`:

ابحث عن:
```tsx
.from('attachments')
```

وغيره إلى اسم الـ bucket الموجود لديك:
```tsx
.from('your-bucket-name')
```

## الخيار 4: تحقق من Console

افتح Console المتصفح (F12) وابحث عن الخطأ الكامل:
- قد يكون هناك تفاصيل أكثر عن المشكلة
- قد يكون خطأ في الصلاحيات أو الـ URL

## ✅ التحقق من نجاح الإنشاء

بعد إنشاء الـ bucket، تحقق:

1. **في Supabase Dashboard:**
   - Storage → Buckets
   - يجب أن ترى "attachments"

2. **جرب رفع ملف يدوياً:**
   - اضغط على bucket "attachments"
   - اضغط "Upload file"
   - اختر ملف
   - إذا نجح، الـ bucket يعمل

3. **جرب في التطبيق:**
   - افتح مهمة
   - حاول رفع ملف
   - يجب أن يعمل الآن

## 🔍 استكشاف الأخطاء

### الخطأ مازال موجود بعد إنشاء الـ bucket

**السبب المحتمل:** الاسم مختلف أو هناك مسافات

**الحل:**
1. احذف الـ bucket القديم
2. أنشئ واحد جديد
3. تأكد من الاسم: `attachments` (بالضبط)

### الخطأ: "Bucket exists but permission denied"

**الحل:**
1. اذهب إلى bucket "attachments"
2. Policies
3. أضف policy للرفع:
```
Operation: INSERT
Target: authenticated
Expression: true
```

---

**جرب الخيار 2 (إنشاء الـ bucket) مرة أخرى بعناية!** 📎
