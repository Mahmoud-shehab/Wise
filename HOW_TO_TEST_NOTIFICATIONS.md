# كيفية اختبار نظام الإشعارات

## 🧪 خطوات الاختبار

### الخطوة 1: التأكد من تشغيل Migration

1. افتح Supabase Dashboard
2. اذهب إلى SQL Editor
3. شغل هذا الأمر للتحقق:

```sql
-- التحقق من وجود جدول الإشعارات
SELECT * FROM notifications LIMIT 1;

-- التحقق من وجود Triggers
SELECT trigger_name, event_object_table 
FROM information_schema.triggers 
WHERE trigger_name LIKE '%notify%';

-- يجب أن ترى:
-- trigger_notify_task_assignment على جدول tasks
-- trigger_notify_task_comment على جدول task_comments
```

### الخطوة 2: تفعيل Realtime

1. في Supabase Dashboard
2. اذهب إلى Settings > API > Realtime
3. تأكد من تفعيل Realtime للجدول `notifications`
4. احفظ التغييرات

### الخطوة 3: اختبار إشعار تعيين المهمة

#### كمدير:

1. سجل دخول كمدير
2. اذهب إلى "جميع المهام"
3. أنشئ مهمة جديدة أو افتح مهمة موجودة
4. عين المهمة لموظف معين
5. احفظ التغييرات

#### كموظف:

1. سجل دخول بحساب الموظف
2. انظر إلى أيقونة الجرس 🔔 في الـ sidebar
3. يجب أن ترى رقم أحمر يظهر عدد الإشعارات الجديدة
4. اضغط على الجرس
5. يجب أن ترى إشعار: "تم تعيين مهمة جديدة لك"

### الخطوة 4: اختبار إشعار التعليق

#### المستخدم الأول (مدير أو موظف):

1. افتح مهمة معينة لموظف آخر
2. اذهب إلى قسم التعليقات
3. أضف تعليق جديد
4. اضغط "إرسال"

#### المستخدم الثاني (المسند إليه):

1. انظر إلى أيقونة الجرس 🔔
2. يجب أن يظهر إشعار جديد
3. اضغط على الجرس
4. يجب أن ترى: "تعليق جديد على مهمتك"

## 🔍 استكشاف الأخطاء

### المشكلة: لا تظهر أيقونة الجرس

**الحل:**
- تأكد من تحديث الصفحة (F5)
- تحقق من أن مكون NotificationDropdown موجود في Layout.tsx

### المشكلة: الإشعارات لا تظهر

**الحل 1: تحقق من Migration**
```sql
-- في Supabase SQL Editor
SELECT COUNT(*) FROM notifications;
-- إذا كان الناتج خطأ، يعني الجدول غير موجود
```

**الحل 2: تحقق من Triggers**
```sql
-- يجب أن يعيد نتائج
SELECT * FROM information_schema.triggers 
WHERE trigger_name = 'trigger_notify_task_assignment';
```

**الحل 3: اختبار يدوي**
```sql
-- أنشئ إشعار يدوياً للاختبار
INSERT INTO notifications (user_id, type, title, content, link)
VALUES (
  'user-id-here',  -- ضع ID المستخدم
  'test',
  'إشعار تجريبي',
  'هذا إشعار للاختبار',
  '/tasks'
);

-- ثم تحقق من ظهوره في الواجهة
```

### المشكلة: الإشعارات لا تتحدث فورياً

**الحل:**
1. تأكد من تفعيل Realtime في Supabase
2. افتح Console المتصفح (F12)
3. ابحث عن أخطاء في console
4. تأكد من وجود رسالة: "Realtime subscription active"

### المشكلة: الإشعارات تظهر لجميع المستخدمين

**الحل:**
```sql
-- تحقق من RLS Policies
SELECT * FROM pg_policies WHERE tablename = 'notifications';

-- يجب أن ترى policy:
-- "Users can view their own notifications"
-- USING (user_id = auth.uid())
```

## 📝 سيناريوهات الاختبار

### سيناريو 1: تعيين مهمة جديدة

```
المدير:
1. إنشاء مهمة جديدة
2. تعيينها لموظف "أحمد"
3. حفظ

الموظف أحمد:
1. يرى إشعار جديد (رقم أحمر على الجرس)
2. يفتح الإشعارات
3. يرى: "تم تعيين مهمة جديدة لك"
4. يضغط على الإشعار
5. ينتقل لصفحة المهمة
```

### سيناريو 2: تغيير المسند إليه

```
المدير:
1. فتح مهمة معينة لـ "أحمد"
2. تغيير المسند إليه إلى "محمد"
3. حفظ

الموظف محمد:
1. يرى إشعار جديد
2. "تم تعيين مهمة جديدة لك"

الموظف أحمد:
1. لا يرى إشعار (لأن المهمة لم تعد له)
```

### سيناريو 3: إضافة تعليق

```
المدير:
1. فتح مهمة معينة لـ "أحمد"
2. إضافة تعليق: "يرجى إنهاء هذه المهمة اليوم"
3. إرسال

الموظف أحمد:
1. يرى إشعار جديد
2. "تعليق جديد على مهمتك"
3. يفتح الإشعار
4. يرى التعليق في صفحة المهمة
```

### سيناريو 4: تعليق من موظف آخر

```
الموظف محمد:
1. فتح مهمة معينة لـ "أحمد"
2. إضافة تعليق: "هل تحتاج مساعدة؟"
3. إرسال

الموظف أحمد:
1. يرى إشعار جديد
2. "تعليق جديد على مهمتك"
3. يفتح ويرد على التعليق
```

## ✅ قائمة التحقق

قبل الإطلاق، تأكد من:

- [ ] Migration تم تشغيله بنجاح
- [ ] جدول notifications موجود
- [ ] Triggers موجودة وتعمل
- [ ] RLS Policies مفعلة
- [ ] Realtime مفعل للجدول
- [ ] أيقونة الجرس تظهر في الـ sidebar
- [ ] الإشعارات تظهر عند تعيين مهمة
- [ ] الإشعارات تظهر عند إضافة تعليق
- [ ] الإشعارات تتحدث فورياً (بدون تحديث الصفحة)
- [ ] عداد الإشعارات غير المقروءة يعمل
- [ ] الضغط على الإشعار ينقل للمهمة
- [ ] تحديد كمقروء يعمل
- [ ] حذف الإشعار يعمل
- [ ] تحديد الكل كمقروء يعمل

## 🎯 نصائح للاختبار

1. **استخدم حسابين مختلفين**: مدير وموظف
2. **افتح نافذتين**: واحدة للمدير وواحدة للموظف
3. **راقب التحديث الفوري**: يجب أن تظهر الإشعارات بدون تحديث
4. **اختبر جميع السيناريوهات**: تعيين، تعليق، تغيير المسند إليه
5. **تحقق من Console**: ابحث عن أخطاء JavaScript

## 📞 إذا لم يعمل

1. تأكد من تشغيل Migration الصحيح
2. تحقق من Supabase Dashboard > Database > Tables
3. يجب أن ترى جدول `notifications`
4. افتح SQL Editor وشغل الأوامر أعلاه
5. تحقق من Console المتصفح للأخطاء
6. تأكد من تفعيل Realtime

## 🎉 النجاح!

إذا رأيت الإشعارات تظهر فوراً عند تعيين مهمة أو إضافة تعليق، فالنظام يعمل بنجاح! 🎊

---

**نظام الإشعارات جاهز ويعمل تلقائياً!** 🔔✨
