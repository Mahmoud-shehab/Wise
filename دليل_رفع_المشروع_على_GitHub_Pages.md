# 🚀 دليل رفع المشروع على GitHub Pages

## المتطلبات الأساسية

### 1. حساب GitHub
- إذا لم يكن لديك حساب، أنشئ واحد على https://github.com

### 2. Git مثبت على جهازك
- تحقق من التثبيت: افتح CMD واكتب `git --version`
- إذا لم يكن مثبتاً، حمّله من https://git-scm.com/download/win

### 3. ملفات البيئة (.env.local)
⚠️ **مهم جداً:** لا ترفع ملف `.env.local` على GitHub!
- يحتوي على معلومات حساسة (Supabase keys)
- تم إضافته تلقائياً لـ `.gitignore`

---

## الخطوات التفصيلية

### الخطوة 1: إنشاء Repository على GitHub

1. اذهب إلى https://github.com
2. اضغط على **"New repository"** (أو علامة + في الأعلى)
3. املأ البيانات:
   - **Repository name:** `wise-task-manager` (أو أي اسم تريده)
   - **Description:** "نظام إدارة المهام لشركة Wise"
   - **Public** أو **Private** (اختر حسب رغبتك)
   - ✅ **لا تختر** "Add a README file"
   - ✅ **لا تختر** "Add .gitignore"
4. اضغط **"Create repository"**

---

### الخطوة 2: ربط المشروع بـ GitHub

افتح CMD في مجلد المشروع وشغل هذه الأوامر:

```bash
# 1. تهيئة Git (إذا لم يكن مهيأ)
git init

# 2. إضافة جميع الملفات
git add .

# 3. عمل Commit أول
git commit -m "Initial commit: Wise Task Manager"

# 4. تغيير اسم الفرع إلى main
git branch -M main

# 5. ربط المشروع بـ GitHub (استبدل USERNAME باسم المستخدم)
git remote add origin https://github.com/USERNAME/wise-task-manager.git

# 6. رفع الملفات
git push -u origin main
```

⚠️ **ملاحظة:** استبدل `USERNAME` باسم المستخدم الخاص بك على GitHub

---

### الخطوة 3: تفعيل GitHub Pages

1. اذهب إلى Repository على GitHub
2. اضغط على **Settings** (الإعدادات)
3. من القائمة الجانبية، اختر **Pages**
4. في قسم **Source**:
   - اختر **GitHub Actions**
5. احفظ التغييرات

---

### الخطوة 4: تشغيل النشر التلقائي

الآن عند كل Push للكود، سيتم النشر تلقائياً!

لتشغيل النشر أول مرة:
1. اذهب إلى تبويب **Actions** في Repository
2. اضغط على **Deploy to GitHub Pages**
3. اضغط **Run workflow**
4. اضغط الزر الأخضر **Run workflow**

انتظر 2-3 دقائق حتى ينتهي النشر.

---

### الخطوة 5: الوصول للموقع

بعد انتهاء النشر، الموقع سيكون متاح على:

```
https://USERNAME.github.io/wise-task-manager/
```

استبدل `USERNAME` باسم المستخدم الخاص بك.

---

## إعداد متغيرات البيئة

### المشكلة:
ملف `.env.local` لا يُرفع على GitHub (للأمان)، لذلك يجب إضافة المتغيرات يدوياً.

### الحل:

#### الطريقة 1: استخدام GitHub Secrets (موصى بها)

1. اذهب إلى **Settings** → **Secrets and variables** → **Actions**
2. اضغط **New repository secret**
3. أضف المتغيرات التالية:
   - `VITE_SUPABASE_URL`: رابط Supabase الخاص بك
   - `VITE_SUPABASE_ANON_KEY`: مفتاح Supabase

4. حدّث ملف `.github/workflows/deploy.yml`:

```yaml
- name: Build
  run: npm run build
  env:
    VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
    VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
```

#### الطريقة 2: استخدام ملف .env.production

أنشئ ملف `.env.production` في جذر المشروع:

```env
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
```

⚠️ **تحذير:** هذا الملف سيكون مرئياً على GitHub!

---

## تحديث الموقع

عند إجراء تعديلات على الكود:

```bash
# 1. إضافة التعديلات
git add .

# 2. عمل Commit
git commit -m "وصف التعديلات"

# 3. رفع التعديلات
git push
```

سيتم النشر تلقائياً خلال 2-3 دقائق!

---

## استكشاف الأخطاء

### الخطأ: "failed to load module"
**الحل:** تأكد من أن `base` في `vite.config.ts` يطابق اسم Repository:
```typescript
base: '/wise-task-manager/',
```

### الخطأ: "404 Not Found"
**الحل:** 
1. تأكد من تفعيل GitHub Pages في Settings
2. تأكد من اختيار **GitHub Actions** كمصدر
3. انتظر بضع دقائق بعد النشر

### الخطأ: "Supabase connection failed"
**الحل:** تأكد من إضافة متغيرات البيئة (الخطوة 5)

### الخطأ: "Permission denied"
**الحل:** 
1. اذهب إلى **Settings** → **Actions** → **General**
2. في **Workflow permissions**، اختر **Read and write permissions**
3. احفظ التغييرات

---

## نصائح مهمة

### 1. الأمان:
- ✅ لا ترفع ملف `.env.local`
- ✅ استخدم GitHub Secrets للمتغيرات الحساسة
- ✅ راجع ملف `.gitignore` قبل الرفع

### 2. الأداء:
- ✅ المشروع يُبنى تلقائياً (optimized)
- ✅ الملفات مضغوطة
- ✅ PWA جاهز للعمل

### 3. التحديثات:
- ✅ كل Push يُنشر تلقائياً
- ✅ يمكن تشغيل النشر يدوياً من Actions
- ✅ يمكن إيقاف النشر التلقائي إذا أردت

---

## الملفات المهمة

### تم إنشاؤها تلقائياً:
- ✅ `.github/workflows/deploy.yml` - ملف النشر التلقائي
- ✅ `.gitignore` - ملفات لا تُرفع على GitHub
- ✅ `vite.config.ts` - محدث بـ base path

### يجب مراجعتها:
- ⚠️ `.env.local` - لا ترفعه!
- ⚠️ `README.md` - أضف وصف للمشروع

---

## الخطوات السريعة (ملخص)

```bash
# 1. إنشاء Repository على GitHub
# 2. في CMD:
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/USERNAME/wise-task-manager.git
git push -u origin main

# 3. على GitHub:
# Settings → Pages → Source: GitHub Actions

# 4. Actions → Deploy to GitHub Pages → Run workflow

# 5. انتظر 2-3 دقائق
# 6. افتح: https://USERNAME.github.io/wise-task-manager/
```

---

## الدعم

إذا واجهت أي مشكلة:
1. تحقق من تبويب **Actions** لرؤية الأخطاء
2. راجع قسم "استكشاف الأخطاء" أعلاه
3. تأكد من تطبيق جميع الخطوات بالترتيب

---

**مبروك! مشروعك الآن على الإنترنت! 🎉**

الموقع: `https://USERNAME.github.io/wise-task-manager/`
